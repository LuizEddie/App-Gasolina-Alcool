import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'App-Gasolina-Alcool',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
